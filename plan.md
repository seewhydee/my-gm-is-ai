# Things to fix

- revealed_hints is collected (encounters.py:83) but dropped.

- then_check is handled for check-bearing rules but not for
  deterministic checks that go via `result`.  What's the right thing?


Missing test coverage for corpus models (from unbiased review of test_corpus.py)
================================================================================

1. state_fields inner structure
   - Validate that state_fields field declarations require 'type' to be one of:
     'boolean', 'number', 'string'.
   - Validate that 'description' is present inside each state_fields entry.

2. Entity cross-type validation (negative tests)
   - A 'feature' entity with 'dialogue_guidelines' should raise ValidationError.
   - An 'item' entity with 'behavior' should raise ValidationError.
   - A non-npc entity with 'dialogue_guidelines' should raise ValidationError.

3. OnEnterEvent direct validation
   - Test OnEnterEvent in isolation (not just through Room).
   - Test 'trigger_dialogue' field is accepted.
   - Test 'set_entity_state' field is accepted.
   - Validate that 'id' is required.

4. TraversalEffect comprehensive coverage
   - Test 'trigger_encounter' effect on an exit's on_traverse.
   - Test 'skip_if' condition on traversal effects.
   - Test 'narrative_skip' on traversal effects.

5. Interaction.condition
   - Test an interaction that includes a 'condition' object gating its availability.

6. AttitudeLimits defaults
   - Test that 'step_per_turn' defaults to 1 when omitted.

7. Mechanic invalid type (negative test)
   - A mechanic with type "draw" (or any value other than "win"/"lose")
     should raise ValidationError.

8. Check.type enforcement
   - Test that 'type' is required on Check.
   - Test that a Check with type other than "roll" raises ValidationError.

9. will_reveal.set_entity_state
   - Test that a WillRevealEntry accepts 'set_entity_state' side effects.

10. spans_rooms room ID validity (integration)
    - In the sample corpus integration tests, verify that every room ID in
      'spans_rooms' references a valid room in the corpus.
